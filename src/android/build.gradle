apply plugin: 'com.android.application'

// to avoid compilesdk errors
android {
    defaultConfig {
        multiDexEnabled true
    }
}

dependencies {
    // Replace compile() with implementation.
    implementation 'com.android.support:customtabs:27.1.1'
}
